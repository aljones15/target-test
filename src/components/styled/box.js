import styled from '@emotion/styled'

const Box = styled.div({
    boxSizing: 'border-box',
    display: 'flex',
    flexGrow: 1,
    flexShrink: 1,
    padding: '0.5rem',
    maxHeight: '50%',
    maxWidth: '50%'
  })

  export default Box